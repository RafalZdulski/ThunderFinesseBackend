package com.thunderfinesse.thunderback.mongodb.configurations;

/**
 * mongodb.configurations contains singleton classes which fulfil function of access points to mongoDB databases
 */

/* TODO (consideration)
    maybe singleton shouldn't be achieved via Enum but via static field?
    with static field there could be added interface or abstract class to extend from
 */